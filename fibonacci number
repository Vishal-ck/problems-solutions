class Solution:
    def fib(self, n: int) -> int:
        def fib(k):
            if k==0: 
                return 0
            if k==1:
                return 1
            else:
                return (fib(k-1)+fib(k-2))
        return fib(n)
